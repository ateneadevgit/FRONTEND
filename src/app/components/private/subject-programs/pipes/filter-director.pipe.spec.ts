import { FilterDirectorPipe } from './filter-director.pipe';

describe('FilterDirectorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDirectorPipe();
    expect(pipe).toBeTruthy();
  });
});
